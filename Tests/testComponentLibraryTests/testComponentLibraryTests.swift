import XCTest
@testable import testComponentLibrary

final class testComponentLibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(testComponentLibrary().text, "Hello, World!")
    }
}
